import 'package:flutter/widgets.dart';
import 'package:topgo_web/widgets/close_widget.dart';

class CloseButton extends StatelessWidget {
  const CloseButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return CloseWidget();
  }
}
